# Gradle / Android build
.gradle/
build/
app/build/
local.properties
*.iml
.idea/
captures/
.cxx/

# Grosse Whisper-Modelle werden von der CI geladen (nicht committen)
app/src/main/assets/models/*.bin
app/src/androidTest/assets/*.bin

# Release-Keystore + Credentials NIEMALS committen (nur in GitHub-Secrets / lokal)
keystore/
*.p12
*.jks
dist/

# OS
.DS_Store
