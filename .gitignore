# Gradle / Android build
.gradle/
build/
app/build/
local.properties
*.iml
.idea/
captures/
.cxx/

# Grosses Whisper-Modell wird von der CI geladen (nicht committen)
app/src/main/assets/models/*.bin

# Release-Keystore + Credentials NIEMALS committen (nur in GitHub-Secrets / lokal)
keystore/
*.p12
*.jks
dist/

# OS
.DS_Store
